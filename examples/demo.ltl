// examples/demo.ltl — runnable demo for ltl_thrift_read

import std::io
use ltl_thrift_read as lib

fn main() {
    io::println("ltl_thrift_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}