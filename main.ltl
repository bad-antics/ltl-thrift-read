// ltl-thrift-read/main.ltl
//
// Apache Thrift compact-protocol decoder.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-thrift-read v0.1.0")
    io::println("Apache Thrift compact-protocol decoder.")
    0
}
